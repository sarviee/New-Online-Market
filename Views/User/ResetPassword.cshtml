@model OnlineMarket.Models.ResetPasswordViewModel

<div class="container">
    <form asp-action="ResetPassword" method="post" class="mt-5">
        <h2>Parolingiz o'zgartirish</h2>
        <div asp-validation-summary="All" class="text-danger"></div>
        <div class="form-group">
            <label asp-for="Password">Yangi parol</label>
            <input class="form-control" r asp-for="Password" type="password" />
            <span asp-validation-for="Password" class="text-danger"class="form-control"></span>

        </div>
        <div class="form-group">
            <label asp-for="ConfirmPassword">Yangi parolni takrorlang</label>
            <input class="form-control"  asp-for="ConfirmPassword" type="password" />
            <span asp-validation-for="ConfirmPassword" class="text-danger"class="form-control"></span>

        </div>
        
        <button type="submit" class="btn btn-primary mt-2">Tahrirlash</button> 
        <a href="/" class="btn btn-warning mt-2">Bekor qilish</a> 
    </form>
</div>

@await Html.PartialAsync("_ValidationScriptsPartial")